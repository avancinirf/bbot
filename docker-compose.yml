version: "3.9"

services:
  app:
    build: .
    container_name: binance_bot_app
    working_dir: /app
    volumes:
      - ./backend:/app
      - ./data:/app/data
      - ./.env:/app/.env:ro
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
